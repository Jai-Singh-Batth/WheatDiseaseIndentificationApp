# WheatDiseaseIndentificationApp
A react native app for identifying wheat diseases by clicking photos from the phone and then sending the photo to the model deployed in google cloud(GCP) which was trained using deep learning.
